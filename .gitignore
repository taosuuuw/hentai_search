# 操作系统与编辑器
Thumbs.db
Desktop.ini
.DS_Store
*.swp
.idea/
.vscode/

# 运行期产物（网关不写盘，这里只是防御性忽略）
*.log
npm-debug.log*
node_modules/
package-lock.json

# 本地私有配置：可能含 PicACG token / 账号，绝不入库
.env
.env.*
picacg*.json
*.secret

# venera 上游参考源码（如需入库请先确认其许可证）
venera_src/

# 发布产物（git archive 打出的 zip 等，不入库）
dist/

# 词典内部索引：由维护者本地生成，只作离线查阅，不入库（用户要求）
DICTIONARY-INDEX.md
