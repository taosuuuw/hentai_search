# hentai搜索 v3.3.0

**发布日期**：2026-09-24
**分支**：`main` ｜ **标签**：`v3.3.0` ｜ **上一版**：`v3.2.0`
**运行方式**：零依赖、零构建。双击根目录 `start-engine.cmd`（Windows）/ `start-engine.command`（macOS）/
`./start-engine.sh`（Linux），或执行 `node tools/gateway.js` 后用浏览器打开 <http://127.0.0.1:8788/>。

> 本版是 3.2.0 之后的迭代归档，三条主线：**启动就能看到结论**、**失败要说清是哪一种失败**、
> **阅读器首屏更快**；同时把「自建中继」这条唯一能打通 e-hentai / pixiv 的链路首次入库。
> 逐条变更另见仓库根目录的 [CHANGELOG.md](../../CHANGELOG.md)。

---

## 一、检索

| 变更 | 说明 |
| --- | --- |
| E-Hentai 封禁不再每次搜索重烧 | 原先只有「点开作品（弹窗 / 阅读器）」那条路记得上游封禁，搜索路径的短路闸门只看网络层失败，于是接下来**每一次检索**都要把三条腿烧满预算才失败。现在两种记忆共享同一个闸门（`ehBanGate()`），并按记录它时的**出口**绑定（`ehBanEgress`）——出口变了（用户开关代理）就不再用旧出口的封禁挡新出口，恢复不必重启网关 |
| 熔断期间不走浏览器直连兜底 | 网关与浏览器在同一条出口上；网关已判出「上游按出口 IP 限流」，浏览器直连面对的是同一面墙，实测要白烧 4–5 秒再同样失败。拿到熔断结论就立刻如实交差（**带原因的失败**，不是「0 条结果」），把时间和预算还给别的源 |
| 修「E-Hentai 返回 0 条」的假成功 | `directAnswered = true` 原先写在封禁判定**之前**：取到封禁页也先置成功标志，紧接着抛错被 `catch` 吃掉而标志没回滚，最后走到函数尾部返回空数组 ⇒ UI 显示「✓ E-Hentai 返回 0 条」。现在先判定、确认是**真页面**之后才置成功标志 |
| 「汉化 / 中文」成为结果页筛选位 | `assets/js/results.js` 的 `R.zhOnly` |
| 跨语言词形补漏 | `assets/js/xlate.js` 补上 `-ism ↔ -ist` 这组同词根不同词性（英 / 法） |

## 二、信息源与网关

- **新增 `/api/warm`**：把 `/api/diag` 那套出口自检提前到**弹出网页时**跑掉，顺手把阅读器要用的图片主机钉好 IP（只做 DNS + TLS 验真，不取内容）。三段并行、各自软超时、整段再套保险丝——**预热绝不能变成新的等待**：第一版串起来写，真机实测 **31.2 秒**才回。刻意**不**对 e-hentai 发检索：取证显示它的封禁窗口正是重试刷出来的。
- **结论只认完整的那一份**：只有「每个目标都真拿到结论」才允许写进与 `/api/diag` 共用的结论缓存（60 秒）；任何一项硬超时（`unknown`）都不许冒充最终结论——把一次超时说成「连网关也打不通」正是误报来源。前端（`assets/js/net.js`、`assets/js/app.js`）网关一连上就让它预热，并**先把网关探明、再做网络自检**，修掉「弹出网页时就看到『部分网点没有连接』」；标题栏也先认网关在不在。
- **阅读器首屏提前预热**（`warmReaderImages`）：nhentai 一次 `/api/reader` 本身只要 **455ms**，但第一张图冷取要 **1468ms**——首屏那 1 秒多全花在图上。现在返回 JSON 时顺手把前 2 页拉进代理缓存。预热是**投机性**的：失败一律吞掉，不进在途合并表、不写缓存，**也不许把主机拉进冷却**。
- **新增 `/api/prefetch`**：阅读器把「接下来几页」的取图地址一次性交给网关，网关按并发上限取回并填进**与 `/api/proxy` 完全同一份缓存**（缓存键刻意用同一把，否则预取全白做）；用户真滑到那页就是毫秒级命中，中继 / 上游配额也不必重复烧。逐条吞错、有整批死线，绝不因预取失败影响主流程。
- **nhentai 图库 JSON 预算重定标**：总预算 20000 → **10000ms**，非私有腿再压 `legCap = 2000ms`。原来最坏路径会给两条 AllOrigins 各 4750ms（实测 12137ms 才回来），而这一发只是取一段 ~3KB 的 JSON。
- **按上游自己的倒计时冷却**：e-hentai 不再每 25 秒撞一次，并把解封时刻记下来；`DEAD_SITE_FIX` 把 e-hentai 与 pixiv 拆开（两者的死因与修法都不同）。自检必须有真死线，撞上上游限流要把封禁窗口记进 `ehBanEgress`；同时明确「直连被 Cloudflare 拦 ≠ 这个源不能用」。
- **wnacg 自检换镜像**：`www.wnacg.com` 首页实测 4900–7200ms，自检改打 `02.cc`。
- **中继不接拷贝漫画 API 节点**（`RELAY_BAD_HOSTS`）：实测中继对它们只回空壳，白烧配额。
- **禁漫域名复用**：5 分钟内优先复用上次选中的域名（`jmPickHost`），换域名时重试 `/chapter` 与章节模板。
- **自建中继首次入库**：本机出口上 e-hentai 与 pixiv **没有任何现成通路**——`e-hentai.org` 系统 DNS 直接 NXDOMAIN、DoH 候选 IP 带真 SNI 直连全超时；pixiv 的 DoH 给 Cloudflare 真段，带真 SNI 直连约 100ms 即 ECONNRESET；**15 条公共中继候选 × 2 个目标 = 0 成功**。本版补齐整条链路：`tools/relay-setup.ps1`（一次性向导：CF Pages 自动部署 / 粘贴已有地址 / 自己的 VPS / 本机自测 / 稍后再说）、`tools/relay-setup.js`（判据与落盘，可断言测试，也是 macOS / Linux 手动入口）、`tools/relay-server.js` / `relay-worker.mjs` / `relay-deno.ts`（VPS / Cloudflare / Deno Deploy 三种实现）、`tools/relay-check.js`（93 条断言）、`tools/relay-deploy.md`、`tools/relay.txt.example`（真正的 `tools/relay.txt` 不入库）。`tools/start-gateway.ps1` 起引擎前调向导，新增 `-RelaySetup` / `-NoRelaySetup` / `-ResetRelaySetup`，就绪后读 `/api/ping` 打印「自建中继：已启用 / 配了但网关没认出来 / 未配置 ⇒ e-hentai / pixiv 搜不到」。向导做**任何网络判断都走 node**：本机实测 PowerShell 5.1 的 `Invoke-WebRequest` 连不上任何外网 HTTPS，同一时刻 Node 全部 200，用它判断可达性会得出错误结论。

## 三、界面与阅读

- **卡片 / 放大器的完整标签**（新增 `assets/js/cardtags.js`）：小卡片与大卡片原本只显示 1–2 个标签，因为两条路都不通——① 检索接口本身不带标签（nhentai 的 `/api/v2/search` 只回数字 `tag_ids`），② 带回来的被 `slice(0,16)` 再取前 6 截断。现在对**进入视口**的卡片按作品 id 从原站补一路**只用于展示**的标签，存进 `it.srcTags`（**绝不写回 `it.tags`**，避免连带改变同系列堆叠与跨源去重）。约束是硬性的：同一时刻最多 2 个在飞、同源两次请求至少隔 280ms、结果落 `localStorage` 30 天、某个源一旦 403 / 429 / 超时则**整源冷却 5 分钟**——宁可标签晚点出现，也不要把出口 IP 打黑（那会连检索一起拖垮）。网关离线 / 该源没有取法 / 缓存已命中 ⇒ 一个请求都不发；失败是静默的。
- **右下角「迅速回顶」**（新增 `assets/js/totop.js`）：滑动超过 `max(480px, 视口高度 × 0.6)` 才出现（太早出现会压住卡片右下角的操作区）；点击走自绘 rAF 340ms `easeOutCubic` 补间（原生 `scroll-behavior:smooth` 在长页面上要 600ms+）；补间期间用户自己一滚就立刻交还控制权；rAF 被挂起时另有兜底定时器，不会「点了没反应」；动效关闭时直接瞬移。
- **搜索框测光阴影层**：`index.html` 的 `.hs-sb-shade` + `assets/css/style.css`。
- **阅读器**：左右单页模式的缩放作用在图片盒子上（`assets/js/reader.js`）；任意缩放都能按住拖动平移；提前把后面的页交给网关批量预取。

## 四、跨平台与运行环境

- 三个平台入口行为一致（停旧引擎 → 起新引擎 → 等 `/api/ping` → 自检 `/api/reader` → 开浏览器）：`start-engine.cmd` / `start-engine.command` / `start-engine.sh`；本版起**首次启动会多问一次「自建中继」**（一次性，选择记在 `tools/.relay-setup.json`，非交互式启动绝不弹向导）。
- 零依赖、零构建不变：没有 `package.json`，`require()` 只用 Node 内置模块。
- Node **18+** 可跑；CDP / Cloudflare 通路（porn-comic、被 CF 拦的 danbooru）需要 Node **22+**；出口「原路」一层要真正走本地代理需要 Node **24+**（`NODE_USE_ENV_PROXY=1` 仅 undici v24+ 生效），18–23 上会静默直连并由第 ② / ③ 层补偿。

---

## 发布前验证（本次实测）

- ✅ **版本号已更新**：`assets/js/core.js` 的 `NS.VERSION = '3.3.0'`，设置弹窗里的版本号由它渲染。
- ✅ **README / CHANGELOG / 本文件同步**：README 版本元信息、网关接口表（补 `/api/warm`、`/api/prefetch`）、目录结构（补 `cardtags.js`、`totop.js`、校验套件与探针）、启动器参数（补 `-RelaySetup` 等）与「一次性设置：自建中继」一节；CHANGELOG 新增完整 `## v3.3.0` 条目。
- ✅ **回归校验套件全绿**：`node tools/check-all.js` —— **9 套断言、489 条、0 失败**（`concept-check` / `cardtags-check` / `scroll-check` / `glass-check` / `reader-check` / `recent-check` / `dict-check` / `gateway-check` / `relay-check`）。
- ✅ **零依赖**：仓库无 `package.json`，`require()` 仅使用 Node 内置模块。
- ✅ **仓库卫生**：`.dsh/`（本机工作区记忆）已从仓库移除并加入 `.gitignore`；`.mnemon/`、`.wrangler/`、`tools/_*`、`tools/stability-run/`、机器可读测量输出（`*-truth-*.json` / `ui-diag.json` / `reader-zoom-*.json` 等）全部不入库；`tools/relay.txt`（含密钥）不入库，仅留 `tools/relay.txt.example` 模板。
- ✅ **无内联凭据**：仓库内无 `.env` / `picacg*.json` / `*.secret`；Pixiv 的 `PHPSESSID` 只存用户本机 `localStorage`。
- ⏳ **未验证**：各信息源在真实网络下的端到端效果。本机出口对 `e-hentai` / `pixiv` **无任何现成通路**（15 条公共中继候选 × 2 目标 = 0 成功），必须自建中继；`nhentai` / `Hitomi` / `wnacg` 仍需经网关的 DoH 或中继。README 与各批注中对应条目仍如实标注为【待实测】。

## 升级注意

- **升级后重启一次网关进程**：预热结论、封禁记忆、在途合并、图片缓存都是**进程内**状态；三个启动器都会自己停旧起新。
- `.dsh/`（工作区记忆技能）**不再入库**：它属于本机记录，已从仓库移除并加入 `.gitignore`；本地文件不受影响。
- **首次启动会多问一次「自建中继」**：不想配置就选「稍后再说」，或启动时加 `-NoRelaySetup`。不配置的话 `e-hentai` 与 `pixiv` 仍然搜不到——这是出口事实，不是本版的回归。
- 页面仍是传统 `<script>` 顺序加载（非 ES Module），`file://` 直接打开也能搜；**在线阅读必须经本地网关**。
- 已知限制未变：porn-comic 依赖本机 Chrome 过 Cloudflare；紳士漫畫 / E-Hentai / Hitomi 为 HTML 解析，站点改版即失效；禁漫部分章节的图片反混淆未实现；自建中继与境内公共中继都是限流资源。
