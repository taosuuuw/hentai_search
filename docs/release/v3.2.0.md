# hentai搜索 v3.2.0

**发布日期**：2026-09-22
**分支**：`main`（由 `3.0` 更名而来） ｜ **标签**：`v3.2.0` ｜ **上一版**：3.0（未单独发布）
**运行方式**：零依赖、零构建。双击根目录 `start-engine.cmd`（Windows）/ `start-engine.command`（macOS）/
`./start-engine.sh`（Linux），或执行 `node tools/gateway.js` 后用浏览器打开 <http://127.0.0.1:8788/>。

> 本版把 3.0 分支上积累的整套改动正式归档，并把分支 `3.0` 更名为 `main` —— 这是 `main` 的首个发布。
> 逐条变更另见仓库根目录的 [CHANGELOG.md](../../CHANGELOG.md)。

---

## 一、检索

| 变更 | 说明 |
| --- | --- |
| 跨语言扩召回 | 新增 `assets/js/xlate.js`：离线词典 0ms 出候选，网关机器翻译（`/api/translate`，es 优先）与离线结果合并进同一次检索；`X.stems()` 补词干变体，修正「机器译文是名词、站点标题不是」 |
| 重新提交即打断 | 网络层 `AbortController` 作用域一次性断开在途请求；逻辑层检索代次让旧链路在每个 `await` 后就地退出。修掉「旧检索把步骤追加到新检索的思维链上」与「旧 `finally` 清掉新检索按钮状态」 |
| 同系列堆叠重做 | 封面指纹 / 命名结构（剥 `[社团] (作者)` 与尾部卷号后比主干）/ 同名指纹（含中英文互译，整串相等）/ 同画师 token 相交 / 标题主干相同；明确**不用**「共同标签数」「标题互相包含」两个会大面积误并的判据 |
| 已知角色优先 | `item._charTier`（真角色 / 系列内容 / 字面子串 / 无关）+ `charFirst()` 稳定分区；汉化优先改为**分区内**次级键，不再把翻译过的无关条目顶到真角色前面 |

## 二、信息源与网关

- **新增 `/api/wnacg/search`**：紳士漫畫分批竞速镜像（每批 3 个，先到先得）+ 跟随镜像重定向 + 记住最近成功镜像与失败主机冷却 + 5 分钟结果缓存；前端优先走它，取不到才退回浏览器那套 HTML 竞速。
- **接入 LectorManga**（西语站 `lector-mangas.lat`，检索参数是 `?search=` 而非 `?q=`），网关在线时自动启用。
- **porn-comic 三条通路重排**为「直连 → 本机 Chrome 过 Cloudflare → 境内中继」+ **通路记忆**（10 分钟）+ **单次取页总预算 20 秒** + 挑战页 4.5 秒判死；空结果只认**站点自述**（标题出现 `no result`）。
- **E-Hentai 区分两种「搜不到」**：出口 IP 限制 与 限流空壳（`HTTP 200 + content-length: 0`）。空壳一律判失败并改经中继换出口，识别到后该主机进 60 秒冷却；仍为空才退到 `/torrents.php?search=`。
- **出口三层运行期重判**（原路代理 → DoH 多解析器钉真 IP → 境内中继）：代理中途挂掉自动改走直连，代理回来也会被认出来，两种都不需要重启网关。

## 三、界面与阅读

- **新增「最近浏览」**：顶栏时钟入口，按「今天 / 昨天 / 日期」分组，只在真的进入作品（放大卡片 / 在线阅读）时记账，仅存本机 `localStorage`（`hs.recent.v1`，上限 200 条 / 60 天）。
- **联想泡泡不再压住思维链**：顺序固定为「搜索框 → 联想泡泡 → 思维链 → 结果」。
- **阅读器**：左右单页模式的缩放改作用在图片盒子上（作用在 `img` 上会被 `overflow:hidden` 裁掉且没有滚动范围）；任意缩放都能按住拖动平移；章节下拉不再用 `scrollIntoView` 把页面顶跑。
- **封面加载**：候选链改为「所有 http(s) 封面：直连 → 网关兜底」，删除主机白名单（白名单失败是安静的，新源 / 换图床就永久单腿）；卡片 / 收藏 / 历史缩略图一律 `loading="eager"`（lazy 在文档非前台时会把 load 事件压住，整片网格停在占位图）；新增 `?coverdiag=1` 封面账本。

## 四、跨平台与运行环境

- 三个平台入口行为一致（停旧引擎 → 起新引擎 → 等 `/api/ping` → 自检 `/api/reader` → 开浏览器）：`start-engine.cmd` / `start-engine.command` / `start-engine.sh`。
- 零依赖、零构建不变：没有 `package.json`，`require()` 只用 `child_process crypto dns fs http https os path tls zlib`。
- Node **18+** 可跑；CDP / Cloudflare 通路（porn-comic、被 CF 拦的 danbooru）需要 Node **22+**；出口「原路」一层要真正走本地代理需要 Node **24+**（`NODE_USE_ENV_PROXY=1` 仅 undici v24+ 生效），18–23 上会静默直连并由第 ② / ③ 层补偿。

---

## 发布前验证（本次实测）

- ✅ **版本号一致**：`assets/js/core.js` 的 `NS.VERSION = '3.2.0'`，设置弹窗里的版本号由它渲染；历史遗留的「3.1」「4.0」字样已全部统一为 3.2.0。
- ✅ **语法检查**：`node --check assets/js/core.js` 通过（Node v24.21.0）。
- ✅ **零依赖**：仓库无 `package.json`，`require()` 仅使用 Node 内置模块。
- ✅ **提交内容与工作树一致**：`git status --short` 为空；tracked 文件清单已逐条核对（73 个文件，含 `assets/dict/ip/*.json` 词典包与 `tools/*`）。
- ✅ **未跟踪项已归位**：`memory/`（工作区记忆）与 `icon-draft/`（空 Univer 草稿容器，trunk 无 unit）已进 `.gitignore`，不入库；`dist/`、`DICTIONARY-INDEX.md` 保持忽略。
- ✅ **无内联凭据**：仓库内无 `.env` / `picacg*.json` / `*.secret`；Pixiv 的 `PHPSESSID` 只存用户本机 `localStorage`。
- ⏳ **未验证**：各信息源在真实网络下的端到端效果（本机 DNS 屏蔽了 nhentai / E-Hentai / Hitomi / wnacg，必须经网关的 DoH 或中继）。README 与各批注中对应的条目仍如实标注为【待实测】。

## 升级注意

- **升级后必须重启一次网关进程**：出口不再粘死的逻辑只在新进程里生效；三个启动器都会自己停旧起新。
- 页面仍是传统 `<script>` 顺序加载（非 ES Module），`file://` 直接打开也能搜；**在线阅读必须经本地网关**。
- 已知限制未变：porn-comic 依赖本机 Chrome 过 Cloudflare；紳士漫畫 / E-Hentai / Hitomi 为 HTML 解析，站点改版即失效；禁漫部分章节的图片反混淆未实现；境内中继是限流资源。
